jwt:
  secret: seokwon
data:
  filename: sample.csv
  disabled: true
logging:
  level:
    org:
      springframework:
        data:
          mongodb:
            core:
              MongoTemplate: DEBUG
spring:
  mongodb:
    embedded:
      storage:
        database-dir: "mongodb"